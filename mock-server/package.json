{
  "name": "ajo-devserver",
  "version": "0.1.0",
  "private": true,
  "description": "Local stand-in for the Sub0 backend. Mirrors the same resource routes and websocket contract so the frontend runs without deploying. Zero dependencies - plain Node.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
